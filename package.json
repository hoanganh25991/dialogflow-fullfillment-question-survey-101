{
  "name": "sample-http",
  "version": "0.0.1",
  "dependencies": {
    "axios": "^0.17.1",
    "firebase-admin": "^5.5.0",
    "redux": "^3.7.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-node7": "^1.5.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.0.0",
    "prettier": "^1.4.4"
  },
  "scripts": {
    "build": "rm -rf dist && babel src --out-dir=dist",
    "buildIndex": "rm -rf _index.js dist && babel index.js --out-file=_index.js",
    "test": "babel-node scripts/run-test"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write --no-semi --print-width 120",
      "git add"
    ]
  }
}
